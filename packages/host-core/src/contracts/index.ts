/**
 * Contract surface — pure types shared with the desktop preload (re-exported
 * through desktop/src/shared/codebase-index.ts) and the WebSocket protocol.
 */
export * from './codebase-index'
export * from './types'
